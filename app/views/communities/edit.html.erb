<%= render 'layouts/navbar' %>
<div class='container'>
  <div class='section'>
    <div class='row'>
      <h1 class="header center orange-text"><%= @community.name %></h1>
      <%= render 'form', community: @community %>
    </div>
    <div class='row'>
      <%= link_to t('.show'), @community %> |
      <%= link_to t('.back'), communities_path %>
    </div>
  </div>
</div>
